.main
  .graph
    .graph__title
      今月の出費(種類別)
      ＊縦軸は値段です。
    .graph__box
      = column_chart(@spending)
  .calender
    = month_calendar(attribute: :created_at, events: @spendings, number_of_month: 1) do |date,spends|
      = date.day
      - spends.each do |spend|
        .money-show
          = spend.sort
          = spend.money
          円
      




          

      

      
      
    
